/// Centralised asset paths so a typo never has to be hunted down across
/// a dozen widgets — change the file location once, here.
abstract final class AssetPaths {
  static const String metroDatabase = 'assets/metro.db';
  static const String metroMapSvg = 'images/tehran-subway.svg';
}
